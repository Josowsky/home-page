import styled from 'styled-components';

import { gutter } from 'shared/constants/constants.style';

export const StyledContainer = styled.div`
  width: 100%;
  max-width: 1272px;
  margin: 0 auto ${gutter(15)};
`;
